import React from 'react'
import {connect} from 'react-redux'


function Repairs (props) {
    return (
        <h1>Repairs</h1>
    )
}

function mapStateToProps(state) {
    return {
        
    }
}

export default connect(mapStateToProps, {})(Repairs)